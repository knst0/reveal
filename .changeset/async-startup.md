---
reveal: patch
---

Open the window immediately instead of waiting for the first decode and a full directory scan; the folder is scanned in the background and dropped or dialog-opened files no longer freeze the interface.
