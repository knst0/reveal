---
reveal: patch
---

Avoid copying the whole file when probing PNG, GIF and WebP for animation, stop cloning the configuration for every settings row, and run "Set defaults" and update checks off the UI thread.
