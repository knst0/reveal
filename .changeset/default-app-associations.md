---
reveal: minor
---

Add a "Set defaults" button in Settings that registers Reveal as a handler for every supported image format. macOS releases now ship a `Reveal.app` bundle, without which Reveal could not be selected as a default application at all.
