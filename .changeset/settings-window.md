---
reveal: minor
---

Open settings in their own window instead of an overlay panel, so the image stays
visible while it is open. Every change is previewed live in the main window as
you make it, and closing with Cancel, Esc or the window button reverts to the
last saved values; Save keeps them and writes them to disk.
