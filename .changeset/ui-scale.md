---
reveal: minor
---

Add an interface scale setting. The whole interface can be rendered at 88%, 100%,
125% or 150%, which is useful on high-density displays where the bars and text
were too small. The choice is stored as `window.ui_scale` in the configuration.
