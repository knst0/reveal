---
reveal: patch
---

Map the generic SVG font families (`sans-serif`, `serif`, `monospace`, `cursive`, `fantasy`) onto fonts that are actually installed, so `<text>` no longer renders blank on systems without Arial or Times New Roman.
