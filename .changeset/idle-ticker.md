---
reveal: patch
---

Slow the frame ticker while the viewer is idle so a static image no longer wakes the CPU 60 times a second, and stop cloning the whole entry list on every random slideshow step.
